---
import Layout from '../layouts/Layout.astro';
import Carousel from '../components/Carousel.astro';
import Slide from '../components/Slide.astro';
import MovieCard from '../components/MovieCard.astro';
import movies from '../data/movies.json';

const heroMovie = movies.find((m) => m.featured) || movies[0];
const newestMovies = [...movies].sort((a, b) => new Date(b.releaseDate).getTime() - new Date(a.releaseDate).getTime()).slice(0, 5);
---

<Layout title="New" htmlId="new-overview" currentNav="new">
    <main class="content-grid snap-top">
        <div class="sticky-header" data-title={heroMovie.title}></div>

        <div class="hero full-width">
            <img src={heroMovie.image} alt={heroMovie.title} />

            <div class="content content-grid">
                <h1 data-title={heroMovie.title}>{heroMovie.title}</h1>
                <div class="rating">{heroMovie.rating}</div>
                <p>
                    {heroMovie.description}
                </p>
                <button class="button radius-medium">Watch now ▶</button>
            </div>
        </div>
        <h1>Newest releases</h1>
        <Carousel>
            {newestMovies.map((movie) => (
                <Slide>
                    <MovieCard movie={movie} />
                </Slide>
            ))}
        </Carousel>
    </main>
</Layout>
